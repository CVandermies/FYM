{% extends "base.html.twig" %}

{% block body %}
    <article>
        <h2>{{ movie.title }}</h2>
            {% if movie.category is not null %}
                <div class="metadata">Dans la catégorie <b>{{ movie.category.label }}</b></div>
            {% endif %}  
            <div class="content">
                <img src="{{ movie.image }}" alt="">
                
                <div>{{ movie.content | raw}}</div>
                <b>{{ movie.rating }} /5</b>

            </div>
        <a href="{{ path('movie_edit', {id: movie.id }) }}" class="btn btn-primary">Editer</a>
        <a href="{{ path('movie_del', {id: movie.id }) }}" class="btn btn-primary">Supprimer</a>
    </article>
    
{% endblock %}